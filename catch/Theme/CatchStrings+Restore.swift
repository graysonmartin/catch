import Foundation
import CatchCore

extension CatchStrings {

    enum Restore {
        static let restoringData = String(localized: "hold on, getting your cats back...")
        static let restoreFailed = String(localized: "couldn't restore your data. try reopening the app?")
    }
}
